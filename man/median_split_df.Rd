% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/median_split.R
\name{median_split_df}
\alias{median_split_df}
\title{Median Split Function for Data Frames}
\usage{
median_split_df(
  df,
  cols = NULL,
  high_label = "High",
  low_label = "Low",
  ordered_factor = FALSE,
  addvalue = FALSE,
  digits = NULL,
  lower = FALSE
)
}
\arguments{
\item{df}{A data frame containing numeric columns to be split.}

\item{cols}{A character vector of column names or a numeric vector of column indices to split (default: all numeric columns).}

\item{high_label}{Label for the group above the median (default: "High").}

\item{low_label}{Label for the group below the median (default: "Low").}

\item{ordered_factor}{Logical; if TRUE, returns an ordered factor (default: FALSE).}

\item{addvalue}{Logical; if TRUE, appends the median value to the labels (default: FALSE).}

\item{digits}{Number of digits to round the median value (default: NULL).}

\item{lower}{Logical; if TRUE, uses ">" for the high group, otherwise ">=" (default: FALSE).}
}
\value{
A data frame with new columns containing the split groups.
}
\description{
Splits specified numeric columns in a data frame into two groups based on the median value.
}
\examples{
df <- data.frame(a = rnorm(100), b = rnorm(100))
median_split_df(df, cols = "a")
median_split_df(df, cols = c("a", "b"), addvalue = TRUE, digits = 2)

}
