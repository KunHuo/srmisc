% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modify_cells.R
\name{modify_cells}
\alias{modify_cells}
\title{Modify values in a data frame with factor handling}
\usage{
modify_cells(df, rows, cols, values)
}
\arguments{
\item{df}{A data frame to modify}

\item{rows}{Numeric/character vector specifying target rows (indices or names)}

\item{cols}{Numeric/character vector specifying target columns (indices or names)}

\item{values}{Vector of values to insert (must match length of rows/cols)}
}
\value{
Modified data frame with updated values and adjusted factor levels
}
\description{
Safely modify values in specified cells of a data frame, including proper
handling of factor levels when updating factor columns.
}
\details{
Features:
\itemize{
\item Handles both numeric indices and named rows/columns
\item Automatically adds new factor levels when needed
\item Preserves existing factor levels and column types
\item Validates input dimensions and existence of rows/columns
}
}
\examples{
# Create sample data
df <- data.frame(
  ID = 1:3,
  Grade = factor(c("A", "B", "C")),
  Score = c(90, 85, 88),
  row.names = c("Row1", "Row2", "Row3")
)

# Modify single cell (numeric index)
modify_cells(df, rows=2, cols=3, values=95)

# Modify multiple cells with row/col names
modify_cells(df,
            rows=c("Row1", "Row3"),
            cols=c("Grade", "Score"),
            values=c("D", 100))

# Add new factor level
modify_cells(df, rows=1, cols="Grade", values="D")

}
