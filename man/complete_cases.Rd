% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/complete-cases.R
\name{complete_cases}
\alias{complete_cases}
\title{Filter Missing Values from Vectors and Data Frames}
\usage{
complete_cases(x, vars = NULL)
}
\arguments{
\item{x}{Input data (vector or data frame)}

\item{vars}{Optional; for data frames: column names/indexes to check for NAs.
If NULL (default), checks all columns.}
}
\value{
Filtered data matching the input type:
\itemize{
\item For vectors: vector with NA values removed
\item For data frames: data frame with complete rows
}
}
\description{
Removes NA values from vectors or filters complete cases (rows with no NA)
in data frames. Supports subsetting specific columns for data frame validation.
}
\examples{
# Vector example
complete_cases(c(1, NA, 3))  # Returns c(1, 3)

# Data frame examples
df <- data.frame(A = c(1, NA, 3), B = c(NA, 5, 6))
complete_cases(df)             # Keeps only complete rows
complete_cases(df, vars = "A") # Keeps rows where column A is not NA

}
