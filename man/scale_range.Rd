% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_to_01.R
\name{scale_range}
\alias{scale_range}
\title{Scale numeric data to a custom range}
\usage{
scale_range(x, vars = NULL, range = c(0, 1), na.rm = TRUE)
}
\arguments{
\item{x}{A numeric vector, matrix, or dataframe containing the data to scale}

\item{vars}{For dataframe input: character vector of column names to scale.
If NULL (default), scales all numeric columns.}

\item{range}{Numeric vector of length 2 specifying the desired output range.
Default is c(0, 1).}

\item{na.rm}{Logical indicating whether to ignore NA values (default TRUE)}
}
\value{
An object of the same class as x with values scaled to specified range
}
\description{
This function scales numeric data to a specified range (default \link{0, 1}).
It handles vectors, matrices, and dataframes. For dataframes, you can specify
which variables to scale.
}
\examples{
# Vector example with default [0,1] range
scale_range(1:10)

# Vector example with custom [-1,1] range
scale_range(1:10, range = c(-1, 1))

# Dataframe example - scale specific columns to [0,100] range
df <- data.frame(a = 1:10, b = 20:11, c = letters[1:10])
scale_range(df, vars = c("a", "b"), range = c(0, 100))

# Matrix example with [1,10] range
mat <- matrix(1:20, ncol = 2)
scale_range(mat, range = c(1, 10))
}
