% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reverse-scoring.R
\name{reverse_scoring}
\alias{reverse_scoring}
\title{Reverse Scoring Function for Vectors and Data Frames}
\usage{
reverse_scoring(x, vars = NULL, min_score = NULL, max_score = NULL)
}
\arguments{
\item{x}{Input data: a numeric vector or data frame containing numeric columns}

\item{vars}{Optional; for data frames: column names/indexes to reverse.
If NULL (default), reverses all numeric columns.}

\item{min_score}{Optional manual override for minimum possible score}

\item{max_score}{Optional manual override for maximum possible score}
}
\value{
Object of the same type as input (vector or data frame) with reverse-scored values
}
\description{
Performs reverse scoring on numeric vectors or specified columns in a data frame.
Commonly used for psychological scales and questionnaire data processing.
}
\examples{
# Vector example
reverse_scoring(1:5)  # Returns c(5,4,3,2,1)

# Data frame example
df <- data.frame(Q1 = 1:5, Q2 = c(0,1,2,3,4))
reverse_scoring(df)

# Specify columns and scoring range
reverse_scoring(df, vars = "Q1", min_score = 1, max_score = 5)

}
